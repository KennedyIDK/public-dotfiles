# Mouse configuration for sway

# You can get the names of your inputs by running: swaymsg -t get_inputs

# Mouse Settings
input "1133:16514:Logitech_MX_Master_3" {
    natural_scroll enabled
    accel_profile flat
}

# Mouse Keybinds
bindsym --whole-window BTN_EXTRA workspace next
# bindsym --whole-window BTN_SIDE workspace prev

